name: Deploy to VPS

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy via SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.VPS_SSH_KEY }}" > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519
          ssh-keyscan -H ${{ secrets.VPS_HOST }} >> ~/.ssh/known_hosts
          ssh -o StrictHostKeyChecking=no ${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }} "cd /var/www/mblog && git fetch origin main && git reset --hard origin/main && (test -d /var/www/wiki && cd /var/www/wiki && git pull || cd /var/www && git clone https://github.com/manabubannai/wiki.git wiki)"
      - name: Clear opcache
        run: |
          curl -sf -H "X-Deploy-Token: mblog-opcache-2026" "https://${{ secrets.VPS_HOST }}/opcache-reset.php" || true
